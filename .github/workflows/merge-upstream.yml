name: Merge
on:
  schedule:
    - cron: '0 0 * * *'
  workflow_dispatch:
jobs:
  merge:
    runs-on: ubuntu-latest
    steps:
      - uses: rekgrpth/merge-upstream@v1
        with:
          upstream: x-v8/ngx_http_upstream_session_sticky_module
